const path = require('path')
const APP_PATH = process.env.APP_PATH || `${__dirname}/../../skel`

module.exports = {
  getSteps(storeName) {
    return require(path.resolve(APP_PATH, 'back', storeName, 'steps'))
  }
}
