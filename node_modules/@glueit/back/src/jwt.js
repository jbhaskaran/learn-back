const jwt = require('jsonwebtoken')

module.exports = {
  sign(secret) {
    const salt = 'test'
    return jwt.sign(secret, salt)
  },
  verify(token) {
    const salt = 'test'
    return jwt.verify(token, salt)
  }
}
